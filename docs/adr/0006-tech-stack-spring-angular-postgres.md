# Use Spring Boot + Angular + Tailwind + PostgreSQL over the initially recommended Next.js/Node stack

The team chose Spring Boot (backend), Angular with Tailwind (frontend), and PostgreSQL (database) for this project, overriding an initial recommendation of Next.js/Node. This was a deliberate preference, not an oversight — a future contributor shouldn't "fix" it by reaching for React/Next.
